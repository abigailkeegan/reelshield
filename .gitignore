# API keys - NEVER commit these
.env

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.pytest_cache/

# Database cache
*.db
data/*.db
# except the sanitized seed cache (movies + warnings + models, no user data)
!data/movie_cache.seed.db

# Trained model artifacts (regenerated by retraining)
data/*.pkl
data/*.zip

# Local tooling and course submission copies
.claude/
submission/

# Docker
.dockerignore

# OS
.DS_Store
Thumbs.db

# Backups and logs
*.abi_backup
*_log.txt
*.log
seed_bulk_errors.txt

# Cache snapshots (regenerated from the live DB)
movies_snapshot.txt
